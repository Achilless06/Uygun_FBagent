"""Brand/landing website for Uygun Georgia.

Runs in the same process as the Telegram bot (see src/main.py) so they share
the same SQLite database and `data/` directory. The website is read-only —
it surfaces products + published FB posts; bot writes them.
"""
